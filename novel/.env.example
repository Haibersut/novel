# 通用配置
SPRING_PROFILE=prod

# 数据库模式（single | dual）
DATABASE_MODE=single

# 单数据库模式配置
DB_NAME=novel
DB_USER=noveluser
DB_PASSWORD=novelpass
DB_ROOT_PASSWORD=root123

# 主数据库配置
PRIMARY_DB_URL=jdbc:mariadb://mariadb:3306/novel?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
PRIMARY_DB_USERNAME=noveluser
PRIMARY_DB_PASSWORD=novelpass
PRIMARY_DB_POOL_SIZE=15

# 应用配置
SERVER_PORT=8081
CORS_ORIGINS=http://localhost:8080
SITEMAP_RUN=false

# 代理配置 (可选)
PROXY_HOST=
PROXY_PORT=7897
PROXY_CLIENT=false
